{
  "name": "thearchitect-max-technology-ip-portfolio",
  "version": "2026.9.12-im25",
  "private": true,
  "description": "Local validation tooling for the public standalone Technology & IP Portfolio.",
  "type": "module",
  "scripts": {
    "test": "node scripts/validate-im.mjs && node scripts/validate-site.mjs && node scripts/validate-public-scope.mjs",
    "validate": "node scripts/validate-im.mjs && node scripts/validate-site.mjs && node scripts/validate-public-scope.mjs",
    "build": "node scripts/build-site.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
