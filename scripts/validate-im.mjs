#!/usr/bin/env node
import fs from 'node:fs';import path from 'node:path';import vm from 'node:vm';
const root=process.cwd(),read=p=>fs.readFileSync(path.join(root,p),'utf8'),exists=p=>fs.existsSync(path.join(root,p)),assert=(c,m)=>{if(!c)throw new Error(m)};
for(const p of ['index.html','portfolio.html','opportunity.html','transaction.html','notice.html']){const h=read(p);assert(h.includes('Information Memorandum'),`IM identity missing: ${p}`);assert(h.includes('rel="canonical"'),`canonical missing: ${p}`);assert(h.includes('name="description"'),`description missing: ${p}`);for(const banned of ['Git blob','Git-blob','SHA-256','repository migration','current-state provenance contract','recreation-cost','quick-sale','GitHub Actions'])assert(!h.includes(banned),`technical/public phrase found in ${p}: ${banned}`)}
for(const p of ['assurance.html','commercialization.html','transfer.html']){const h=read(p);assert(h.includes('noindex'),`legacy page not noindex: ${p}`)}
new vm.Script(read('assets/im-20260905.js'),{filename:'assets/im-20260905.js'});assert(read('README.md').includes('commercial rather than technical'),'README positioning missing');assert(!exists('.github/workflows'),'GitHub Actions workflows prohibited');assert(read('LICENSE').includes('All Rights Reserved'),'license boundary');console.log(JSON.stringify({site:'THEARCHITECT_MAX Information Memorandum',successful:true,publicAssets:67,publicMode:'commercial-information-memorandum'},null,2));
